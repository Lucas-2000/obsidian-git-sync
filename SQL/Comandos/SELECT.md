O comando SELECT serve para retornar os registros da uma tabela.

Sua sintaxe:

SELECT * FROM nome_tabela;

Obs: o * pode ser substituído pelas colunas desejadas, mantendo ele vai retornar todas as colunas.